CREATE CONSTRAINT ON (petType:PetType) ASSERT petType.name IS UNIQUE;

LOAD CSV WITH HEADERS FROM "file:///pet-type.csv" AS petTypes
CREATE (
    petType:PetType
    { name: petTypes.name }
)


CREATE CONSTRAINT ON (petBreed:PetBreed) ASSERT petBreed.name IS UNIQUE;

LOAD CSV WITH HEADERS FROM "file:///dog-breed.csv" AS petBreeds
MATCH (dog:PetType {name: "Dog"})
CREATE (
    petBreed:PetBreed
    { name: petBreeds.name }
)
MERGE(dog)<-[r:HAS_TYPE]-(petBreed) RETURN dog,petBreed

LOAD CSV WITH HEADERS FROM "file:///cat-breed.csv" AS petBreeds
MATCH (cat:PetType {name: "Cat"})
CREATE (
    petBreed:PetBreed
    { name: petBreeds.name }
)
MERGE(cat)<-[r:HAS_TYPE]-(petBreed) RETURN cat,petBreed




LOAD CSV WITH HEADERS FROM "file:///fish-breed.csv" AS petBreeds
MATCH (fish:PetType {name: "Fish"})
CREATE (
    petBreed:PetBreed
    { name: petBreeds.name }
)
MERGE(fish)<-[r:HAS_TYPE]-(petBreed) RETURN fish,petBreed




LOAD CSV WITH HEADERS FROM "file:///rabbit-breed.csv" AS petBreeds
MATCH (rabbit:PetType {name: "Rabbit"})
CREATE (
    petBreed:PetBreed
    { name: petBreeds.name }
)
MERGE(rabbit)<-[r:HAS_TYPE]-(petBreed) RETURN rabbit,petBreed



LOAD CSV WITH HEADERS FROM "file:///bird-breed.csv" AS petBreeds
MATCH (bird:PetType {name: "Bird"})
CREATE (
    petBreed:PetBreed
    { name: petBreeds.name }
)
MERGE(bird)<-[r:HAS_TYPE]-(petBreed) RETURN bird,petBreed



LOAD CSV WITH HEADERS FROM "file:///hamster-breed.csv" AS petBreeds
MATCH (hamster:PetType {name: "Hamster"})
CREATE (
    petBreed:PetBreed
    { name: petBreeds.name }
)
MERGE(hamster)<-[r:HAS_TYPE]-(petBreed) RETURN hamster,petBreed